stages:
        - configuration
        - build_debian-ncp
        - build_lamp
        - build_nc
        - build_ncp
sudo: required
language: generic
addons:
  apt:
    packages:
        - docker-ce #Upgrade Docker to 18.06 version that supports experimental features
services:
        - docker
env:
        - TEST_SUITE="touch new"
        - TEST_SUITE="ls ."
jobs:
   include:
        - stage: configuration
          env: null
          script: 
                - ./.travis/main.sh #configuration script for travis machine
                - touch Hello

        - stage: build_debian-ncp
          script: 
                - ls
                - ${TEST_SUITE}
                - ls
